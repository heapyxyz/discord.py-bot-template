[project]
name = "discord.py-bot-template"
dynamic = ["version"]
description = "A simple Discord.py bot template with cogs"
requires-python = ">=3.11"
dependencies = [
    "discord.py>=2.7.1",
    "python-dotenv>=1.2.2",
]
